---
'@velocity-exchange/vaults-sdk': minor
---

Bring the vaults SDK into the monorepo as `packages/vaults-sdk` (was the external
`@velocity-exchange/vaults-sdk` npm package). Consumers now depend on it via
`workspace:*`.
