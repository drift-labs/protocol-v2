# @velocity-exchange/vaults-sdk

## 0.1.0

### Minor Changes

- Bring the vaults SDK into the monorepo as `packages/vaults-sdk` (was the external `@velocity-exchange/vaults-sdk` npm package). Consumers now depend on it via `workspace:*`.
