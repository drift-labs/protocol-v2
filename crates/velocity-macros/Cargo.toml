[package]
name = "velocity-macros"
version = "0.1.0"
edition = "2021"
description = "Procedural macros for Velocity zero-copy account structs."
license = "Apache-2.0"

[lib]
proc-macro = true

[dependencies]
syn = "1.0"
quote = "1.0"
